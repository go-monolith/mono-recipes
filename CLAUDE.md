# CLAUDE.md

## Build Go executable binary
- Always output a single binary in the `./bin` directory relative to the sub project directory.
- The binary name should match the project name (e.g., `background-jobs-demo` for the `projects/background-jobs-demo` project).